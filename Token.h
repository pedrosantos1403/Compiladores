#include <bits/stdc++.h>

using namespace std;

class Token{

    public:

        // Tag do Token é definida apenas uma vez
        int tag;

        vector<string> infos;

        Token(int t);

        string toString();

        // Criar um método para adicionar informações em infos
        
};